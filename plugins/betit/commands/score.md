---
description: Show the betit scoreboard
---

Run `python3 ${CLAUDE_PLUGIN_ROOT}/scripts/betit.py status` and show the output verbatim in a code block. Add one short trash-talk or humble line depending on the record. Nothing else.
