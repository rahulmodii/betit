---
description: User verdict — Claude lost the pending bet
argument-hint: [what went wrong]
---

The user has ruled the pending bet a LOSS. Do not argue or justify. Run `python3 ${CLAUDE_PLUGIN_ROOT}/scripts/betit.py lose "$ARGUMENTS"` and show the result. Then fix whatever the user said was wrong.
