---
description: User verdict — Claude won the pending bet
argument-hint: [optional note]
---

The user has ruled the pending bet a WIN. Run `python3 ${CLAUDE_PLUGIN_ROOT}/scripts/betit.py win "$ARGUMENTS"` and show the result. One line of celebration max.
